// Archivo de traducción de seudocódigo a C++
#include <program1.h>
//#include "./2017/Fechador2/Fechas.h"
/**
*    Enunciado: Procedimiento que intercambia los valores de dos variables pasadas por referencia.
*/
plantilla(Tipo)
procedimiento interc(Tipo porRef x, Tipo porRef y) {
Tipo z = x;
x = y;
y = z;
}
implementa procedimiento interc(doble porRef,doble porRef);
implementa procedimiento interc(real porRef,real porRef);
//implementa procedimiento interc(Fecha porRef,Fecha porRef);
implementa procedimiento interc(entero porRef,entero porRef);
