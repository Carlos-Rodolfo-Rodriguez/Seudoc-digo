#include <program1.h>
/**
*    Enunciado: 
*/
principal                                                       // Unidad de programa principal
largo masa,combustible = 0;
iterar
    leer(masa);
    salirSi(cin.eof());
    combustible += masa / 3 - 2;
    finIterar
mostrar << combustible << salto;
finPrincipal                                                    // Fin de unidad de programa principal
