#include "operLista.h"

plantilla(Tipo)
funcion vectorDin(Tipo) menosPrimero(vectorDin(Tipo) v) {
vectorDin(Tipo) resul = v;
borra1EleVDin(resul,0);
regresa(resul);
}
implementa funcion vectorDin(Grilla2D) menosPrimero(vectorDin(Grilla2D));
