#include "Interseccion.h"
plantilla(Tipo)
funcion vectorDin(Tipo) interseccion(vectorDin(Tipo) porRef uno,vectorDin(Tipo) porRef dos) {
vectorDin(Tipo) resul;
//mostrar << "interseccion de "<< tamanio(uno) << " y "<< tamanio(dos) << salto;
entero sinSigno cta = 0;
paraCadaValor(un,uno)
    si(estaBin2(dos,un)) agregaEleVDin(resul,un);
//        un.muestra();
//    si(esMult(++cta,1000)) mostrar << cta << tabulado;
//    si(esMult(cta,5000)) mostrar << salto;
    finPara
regresa(resul);
}
implementa funcion vectorDin(Grilla2D) interseccion(vectorDin(Grilla2D) porRef,vectorDin(Grilla2D) porRef);
