#include <program1.h>                                           // Archivo de traducción de seudocódigo a C++
/**
*    Enunciado: Dados 2 enteros, sumarlos
*/

principal                                                       // Unidad de programa principal
largo largo entero N, i, aux
    ,suma = 0;
leer(N);
largo largo entero v[N];
variar(i,0,N-1,1)    
    leer(v[i]);
    suma += v[i];
    finVariar
finPrincipal
