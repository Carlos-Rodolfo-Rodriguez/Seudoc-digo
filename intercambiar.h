#ifndef INTERCAMBIAR_H
#define INTERCAMBIAR_H
#include <program1.h>
/**
*    Procedimiento que intercambia los valores de dos variables pasadas por referencia.
*/
plantilla(Tipo)
procedimiento interc(Tipo porRef, Tipo porRef);
#endif
